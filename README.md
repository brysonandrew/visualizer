# Auroscuro Visualizer

Audio-reactive visualizer for **Russian phonk / gym music**, built with **Nuxt 3** on the frontend and a small **Node/TSX backend** for generating noise textures.

You drag in:

- 🎧 an **audio file** (your track)  
- 🎇 an **image** (cover art / fighter shot / ritual sigil)

…and the app does:

- Bass-reactive **brightness + scale**
- Split **bass vs mids** for more musical motion
- **Edge glow** ring on kicks
- **Center glow** breathing with mids
- **Grain overlay** that thickens with intensity

Perfect for turning your Auroscuro tracks into TikTok / Reels / Shorts / YouTube visual loops.

---

## Tech Stack

**Frontend**

- [Nuxt 4.20](https://nuxt.com/) (Vue 3, `<script setup>`)
- [TypeScript](https://www.typescriptlang.org/)
- [Tailwind CSS 3](https://tailwindcss.com/)
- Web Audio API (`AudioContext`, `AnalyserNode`)
- Custom composables:
  - `useAudioVisualizer`
  - `useBeatBrightness`
  - `useEdgeGlow`
  - `useCenterGlow`
  - `useGrainOverlay`
  - `useTempUrl`
  - `useSpacebar`
- `clsx` for tiny class name logic
- Notification system via `useNotification()`

**Backend**

- Node + [tsx](https://github.com/esbuild-kit/tsx) to run TypeScript/ESM scripts
- Noise generator scripts (e.g. `generate-noise.ts`, `generate-auroscuro-noise.ts`) that output PNG textures used by the frontend (`/public/white-noise.png`).

---

## Project Structure

Roughly:

```txt
.
├─ frontend/               # Nuxt app
│  ├─ app.vue
│  ├─ nuxt.config.ts
│  ├─ pages/
│  ├─ composables/
│  │   ├─ useAudioVisualizer.ts
│  │   ├─ useBeatBrightness.ts
│  │   ├─ useEdgeGlow.ts
│  │   ├─ useCenterGlow.ts
│  │   ├─ useGrainOverlay.ts
│  │   ├─ useTempUrl.ts
│  │   └─ useSpacebar.ts
│  ├─ assets/
│  │   └─ css/tailwind.css
│  ├─ public/
│  │   └─ white-noise.png        # generated by backend noise scripts
│  └─ ...
└─ backend/
   ├─ white-noise.ts
   ├─ auroscuro-noise.ts
   └─ ...

  